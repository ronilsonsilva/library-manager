# Placeholder Secret only. Replace values in the cluster; do not commit production credentials.
apiVersion: v1
kind: Secret
metadata:
  name: library-manager-api
  labels:
    app.kubernetes.io/name: library-manager
    app.kubernetes.io/component: api
type: Opaque
stringData:
  ConnectionStrings__Postgres: "Host=REPLACE_WITH_POSTGRES_HOST;Port=5432;Database=library_manager;Username=REPLACE_WITH_USERNAME;Password=REPLACE_WITH_PASSWORD"
  ConnectionStrings__Redis: "REPLACE_WITH_REDIS_HOST:6379"
