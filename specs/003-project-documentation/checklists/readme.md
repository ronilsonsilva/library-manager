# README Delivery-Quality Checklist: Project Documentation

**Purpose**: Requirements-quality review of whether `spec.md` mandates a production-quality root `README.md` that an evaluator can follow as challenge delivery, local run, API, architecture, test, and operations guide—without testing whether the current README already does those things.
**Created**: 2026-08-27
**Feature**: [spec.md](../spec.md)
**Depth**: Standard (challenge-compliance gate for documentation requirements)
**Audience / timing**: Reviewer, before `/speckit-implement` rewrites the root guide

**Note**: This custom checklist is generated by the `/speckit-checklist` command based on feature context and requirements.
**Review Ownership**: This checklist is a reviewer-owned requirements-quality review artifact. Mark an item `[x]` only when the reviewer determines the requirements-quality criterion is satisfied.
**Marker Semantics**: `[x]` means the criterion has been reviewed and satisfied for requirements quality. It does not mean implementation work is complete.

## Requirement Completeness

- [ ] CHK001 Are all challenge-delivery topics mandated as root-guide content (prerequisites, local startup, migrations, tests, environment variables, concurrency, idempotency, cache, audit, known limitations, production evolution, and 2–11 replica correctness) rather than optional extras? [Completeness, Spec §FR-001, §FR-010, §FR-012, §FR-013, §FR-017–FR-022, §FR-027, §FR-037, §FR-038]
- [ ] CHK002 Does the spec require Getting Started to include clone, build, docker compose, shutdown, and clean-volume commands plus local API, Swagger, Keycloak, and health URLs that match repository configuration? [Completeness, Spec §FR-010, §FR-032, User Story 1]
- [ ] CHK003 Does the spec require every current-controller production endpoint to be documented with method, route, purpose, authentication/authorization, headers (including the loan-create idempotency header), request, success status, and relevant error statuses? [Completeness, Spec §FR-014, §FR-015, §FR-016, §FR-017]
- [ ] CHK004 Does the spec require authentication documentation covering Keycloak’s role, OIDC resource-server model (API never issues tokens), Swagger Authorization Code with PKCE, and the Librarian policy? [Completeness, Spec §FR-011]
- [ ] CHK005 Does the spec require database documentation covering PostgreSQL’s role, schema/EF migrations, the migration command, contributor schema-version workflow, and PostgreSQL as source of truth? [Completeness, Spec §FR-013, §FR-021]
- [ ] CHK006 Does the spec require concurrency documentation covering the last-copy race, why read-check-write is unsafe, the atomic PostgreSQL conditional update conceptually, transaction boundaries, and return/cancel (and total-copy) concurrency? [Completeness, Spec §FR-018]
- [ ] CHK007 Does the spec require idempotency documentation covering unique endpoint/key, canonical hash, same-request replay, different-payload conflict, concurrent key behavior, and rollback after unexpected failure? [Completeness, Spec §FR-017]
- [ ] CHK008 Does the spec require audit documentation covering business audit versus technical logs, actor, UTC time, correlation id, contextual payload, and same-transaction pairing with the mutation? [Completeness, Spec §FR-020]
- [ ] CHK009 Does the spec require Redis documentation covering cache-aside, key convention, TTL, PostgreSQL fallback (only if tests confirm it), post-commit invalidation, deactivation invalidation, and Outbox recovery of missed invalidation? [Completeness, Spec §FR-021, §FR-022]
- [ ] CHK010 Does the spec require Outbox documentation covering transactional persistence, background worker, competing-consumer claim/lock, lease, retry/backoff, at-least-once delivery, idempotent consumer, and multi-replica safety as an implemented decision (not future work)? [Completeness, Spec §FR-022]
- [ ] CHK011 Does the spec require testing documentation covering unit versus integration strategy, Testcontainers, real PostgreSQL, real Redis, in-process test host / WebApplicationFactory, two-host concurrent last-copy, and existing concurrency / idempotency / Outbox / Redis-resilience scenarios? [Completeness, Spec §FR-027–FR-031]
- [ ] CHK012 Does the spec require observability documentation covering structured logs, correlation id, traces, custom metrics (only those the service records), OpenTelemetry configuration, and health checks with liveness distinguished from readiness? [Completeness, Spec §FR-025, §FR-026, §SC-009]
- [ ] CHK013 Does the spec require Docker documentation covering the application image definition, Compose services, and clean startup/reset/health commands that match the repository? [Completeness, Spec §FR-032]
- [ ] CHK014 Does the spec require Kubernetes documentation covering Deployment, Service, ConfigMap, Secret references, requests/limits, probes, and external PostgreSQL/Redis/identity assumptions? [Completeness, Spec §FR-033]
- [ ] CHK015 Does the spec require security documentation covering JWT validation, PKCE, parameterized SQL (safe interpolation versus unsafe concatenation), dependency auditing, and secrets policy (no invented or committed production secrets)? [Completeness, Spec §FR-012, §FR-034, §FR-035]

## Requirement Clarity

- [ ] CHK016 Is “followed from a clean machine” defined as using only the root guide (clone, run, authenticate, call the API) without hunting the source tree or other specs? [Clarity, Spec §FR-040, §SC-003, §SC-011]
- [ ] CHK017 Is the loan-create idempotency header required to be documented by its actual HTTP header name (not only as a generic “loan-create key”)? [Ambiguity, Spec §FR-017]
- [ ] CHK018 Does “competing-consumer locking as implemented” require naming the actual claim mechanism (for example `SKIP LOCKED` / lease claim fields) rather than a generic lock sentence? [Ambiguity, Spec §FR-022]
- [ ] CHK019 Does the spec require an explicit statement that application-level and Redis locks are unnecessary for the last-copy invariant? [Clarity, Spec §FR-018, §SC-007]
- [ ] CHK020 Is 2–11 replica correctness required to be explained from database and Outbox guarantees (stateless instances, inventory/key uniqueness, same-transaction audit/outbox, competing workers, idempotent consumers, cache never authorizing loans) rather than from Kubernetes replica count? [Clarity, Spec §FR-019, §SC-007]
- [ ] CHK021 Is Redis required to be described exclusively as a performance optimization, consistent with PostgreSQL remaining the system of record in Database, Cache, Concurrency, and Replica sections? [Clarity, Consistency, Spec §FR-013, §FR-019, §FR-021]
- [ ] CHK022 Are liveness and readiness required to be distinguished, including a prohibition on claiming liveness depends on PostgreSQL or Redis if it does not? [Clarity, Spec §FR-026]
- [ ] CHK023 Does the spec require avoiding duplicated major sections while still covering every mandated topic, with tables preferred for endpoints, environment variables, metrics, and test matrices? [Clarity, Spec §FR-040]

## Requirement Consistency

- [ ] CHK024 Are method, route, authorization, and status-code documentation required to match current controllers and OpenAPI, using the implemented book-update method and not assuming PATCH? [Consistency, Spec §FR-002, §FR-004, §FR-016, §SC-001]
- [ ] CHK025 Is a report-do-not-guess rule required when repository and OpenAPI disagree, so the guide cannot invent a blended contract? [Consistency, Spec §FR-004, §SC-012]
- [ ] CHK026 Are Password Grant / Resource Owner Password Credentials and Direct Access Grants explicitly prohibited as documented login or smoke paths? [Consistency, Spec §FR-011, §SC-006]
- [ ] CHK027 Are environment-variable names required to come from actual application configuration and Docker/Kubernetes files, labeled when compose-only or Kubernetes-only, with no invented names or secret values? [Consistency, Spec §FR-012, Edge Cases, §SC-002]
- [ ] CHK028 Are unsupported claims (invented endpoints, statuses, guarantees, live-cluster deployment, manufactured limitations, untested localization) prohibited consistently across quality, Kubernetes, limitations, and localization rules? [Consistency, Spec §FR-002, §FR-024, §FR-033, §FR-037, §SC-010, §SC-013]

## Acceptance Criteria Quality

- [ ] CHK029 Can presence of every mandated topic be objectively verified via a clickable English-only table of contents that reaches those sections? [Measurability, Spec §FR-001, §FR-005, §SC-004, §SC-005]
- [ ] CHK030 Can “no stale test counts” and “commands/examples are valid” be objectively verified (scenario descriptions preferred; clone/build/compose names and addresses match the repository)? [Measurability, Spec §FR-010, §FR-027, §FR-032, §SC-003, §SC-008]
- [ ] CHK031 Can a reviewer complete a first-pass evaluation (run, authenticate, call a mutation and a query, run tests, explain last-copy and loan-key uniqueness) using only the root guide? [Acceptance Criteria, Spec §SC-011]

## Scenario Coverage

- [ ] CHK032 Are test-only endpoints (including `/security` probes and unexpected-error testing routes) required to be excluded from the public API table? [Coverage, Spec §FR-014, User Story 2]
- [ ] CHK033 Are sequential and concurrent idempotency tests required as separately documented scenarios with asserted database state? [Coverage, Spec §FR-030]
- [ ] CHK034 Is a dedicated two-host concurrent last-copy subsection required (two independent API hosts sharing one PostgreSQL database, one remaining copy, one success and one rejection, no negative inventory)? [Coverage, Spec §FR-029]
- [ ] CHK035 Are Outbox test requirements specified for transactional persistence, successful processing, failure/retry, lease expiration, and multiple-worker safety as actually tested? [Coverage, Spec §FR-031]
- [ ] CHK036 Does the spec explicitly require named authentication and health test matrices, or only a generic integration-test matrix that includes those rows if corresponding tests exist? [Gap, Spec §FR-027, §FR-028]

## Edge Case Coverage

- [ ] CHK037 Is `GET /books/{id}/history` required to be documented only if it remains an intentional production alias, and omitted if removed? [Edge Case, Spec §FR-016]
- [ ] CHK038 Is cache failure / PostgreSQL fallback required to be documented only when current tests confirm that behavior? [Edge Case, Spec §FR-021]
- [ ] CHK039 Are known limitations required to be real current limitations (not manufactured), and production-evolution items required not to imply they are needed for challenge correctness? [Edge Case, Spec §FR-037, §FR-038]

## Non-Functional Requirements

- [ ] CHK040 Does the spec require concise diagrams for architecture, loan concurrency, and Outbox, and prohibit claiming a Kubernetes cluster was deployed when only manifests exist? [NFR, Spec §FR-033, §FR-040, §SC-010]

## Dependencies & Assumptions

- [ ] CHK041 Is the documentation-only scope (root `README.md` unless a real code/OpenAPI mismatch is found) stated so implementers do not treat this checklist as a license to change runtime behavior? [Assumption, Spec §FR-001, §FR-004]

## Notes

- Mark items `[x]` only after review confirms the requirement-quality criterion is satisfied
- Leave items unchecked when they still require clarification, correction, or reviewer evaluation
- `/speckit-implement` reads checklist checkbox state as a gate and must not modify markers
- `checklists/requirements.md` has a separate built-in lifecycle maintained by `/speckit-specify` and `/speckit-clarify`
- CHK017, CHK018, and CHK036 flag spec wording that may need a small clarify before README authoring
- This feature has no `plan.md` or `tasks.md` yet; items are traced to `spec.md` only
- Add comments or findings inline
- Link to relevant resources or documentation
- Items are numbered sequentially for easy reference
